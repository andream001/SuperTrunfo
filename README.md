# SuperTrunfo

## Compiling the Program

To compile the `main.c` file, use the following command in your terminal:

```sh
gcc -o supertrunfo main.c
```

This will generate an executable file named `supertrunfo`.

## Running the Program

After compiling, you can run the program using:

```sh
./supertrunfo
```